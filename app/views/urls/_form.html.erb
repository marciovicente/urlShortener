<%= form_for(@url, :remote => true) do |f| %>
  <% if @url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@url.errors.count, "error") %> prohibited this url from being saved:</h2>

      <ul>
      <% @url.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :url, :placeholder => "Digite a URL", :autocomplete => :off, :required => true%>
  </div>
 
  <div class="actions">
    <button class="btnSubmit">Go!</button>
  </div>
<% end %>
